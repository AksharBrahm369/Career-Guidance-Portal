// Stub for the `server-only` package so unit tests can import server modules.
// In real builds, Next.js resolves the actual `server-only` package which throws
// if imported from a client component bundle.
export {};
