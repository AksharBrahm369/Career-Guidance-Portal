import { ComingSoon } from "@/components/placeholder";

export default function AdminFetchPage() {
  return (
    <ComingSoon
      title="Fetch Manager"
      description="AI-assisted course fetch with exclusion list, scope selector, and preview panel arrives in M2."
    />
  );
}
