import { ComingSoon } from "@/components/placeholder";

export default function AdminDashboardPage() {
  return (
    <ComingSoon
      title="Admin Dashboard"
      description="Stats, pending review count, last fetch timestamp, and quick actions land in M2."
    />
  );
}
