import { ComingSoon } from "@/components/placeholder";

export default function AdminReviewPage() {
  return (
    <ComingSoon
      title="Review Queue"
      description="Inline editing, approve, reject, publish, and bulk actions arrive in M2."
    />
  );
}
