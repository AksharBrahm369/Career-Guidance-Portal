import { ComingSoon } from "@/components/placeholder";

export default function AssessmentEntryPage() {
  return (
    <ComingSoon
      title="Student Assessment"
      milestone="M4"
      description="Three modules — Aptitude, Innate Strengths (MI framework), Academic Interests — plus the scoring algorithm and result screen arrive in M4."
    />
  );
}
